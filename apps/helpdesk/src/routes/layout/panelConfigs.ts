export const expandedSidebarPanelConfig = {
    defaultSize: 240,
    minSize: 240,
    maxSize: 350,
    prioritise: true,
}

export const collapsedSidebarPanelConfig = {
    defaultSize: 48,
    minSize: 48,
    maxSize: 48,
    prioritise: true,
}

export const mainPanelConfig = {
    defaultSize: Infinity,
    minSize: 300,
    maxSize: Infinity,
}
