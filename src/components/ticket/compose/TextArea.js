import React from 'react'

const TextArea = React.createClass({
    render() {
        return (
            <div className="TextArea contenteditable">This area is editable</div>
        )
    }
})

export default TextArea