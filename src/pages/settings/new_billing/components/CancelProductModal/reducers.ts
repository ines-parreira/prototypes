import {CancellationReasonsState} from './types'

export const DEFAULT_STATE: CancellationReasonsState = {
    primaryReason: null,
    secondaryReason: null,
    otherReason: null,
} // the reducer is to be added in the future.
