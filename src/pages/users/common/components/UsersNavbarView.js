import React from 'react'
import ViewNavbarView from '../../../common/components/ViewNavbarView/'

const UsersNavbarView = (props) => (
    <ViewNavbarView viewType="user-list" {...props} />
)

export default UsersNavbarView
