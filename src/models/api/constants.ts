//$TsFixMe remove once replaced with enum OrderDirection
export const ORDER_DIRECTION = Object.freeze({
    ASC: 'asc',
    DESC: 'desc',
})

//$TsFixMe remove once replaced with enum MetaSortOptions
export const META_SORT_OPTIONS = Object.freeze({
    RELEVANCE: 'relevance',
})
