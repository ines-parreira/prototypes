{
    "name": "@repo/types",
    "version": "1.0.0",
    "description": "Non domain specific types for the monorepo",
    "private": true,
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "format:check": "prettier --check .",
        "format:fix": "prettier --write .",
        "lint": "oxlint --quiet",
        "lint:unused": "knip --directory ../.. --workspace @repo/types --no-progress",
        "typecheck": "tsc -p ./tsconfig.json --noEmit true"
    },
    "peerDependencies": {},
    "devDependencies": {
        "@repo/config": "workspace:*",
        "@trivago/prettier-plugin-sort-imports": "catalog:prettier",
        "oxlint": "catalog:oxlint",
        "prettier": "catalog:prettier",
        "typescript": "catalog:typescript"
    }
}
