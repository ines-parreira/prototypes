import { Banner } from '@gorgias/axiom'

export default function MaxFieldsImportedBanner() {
    return (
        <Banner type="info">
            Maximum number of metafields imported. Remove one from your list in
            the table before importing a new one.
        </Banner>
    )
}
