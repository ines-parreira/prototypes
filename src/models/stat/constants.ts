// $TsFixMe fallback for StatType remove on removing flow
export const StatType = Object.freeze({
    Number: 'number',
    String: 'string',
    Percent: 'percent',
    Delta: 'delta',
    Duration: 'duration',
})
