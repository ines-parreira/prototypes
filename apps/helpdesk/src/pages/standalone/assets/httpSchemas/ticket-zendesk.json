{
    "url": "https://{{subdomain}}.zendesk.com/api/v2/tickets",
    "body": {
        "ticket": {
            "comment": {
                "html_body": "{{ticket.messages[-1]['body_html']}}"
            },
            "priority": "high",
            "subject": "Handover: {{ticket.subject}}",
            "requester": {
                "email": "{{ticket.customer.email}}",
                "name": "{{ticket.customer.email}}"
            }
        }
    },
    "headers": {
        "Authorization": "Basic {{basicToken}}"
    }
}
