import React from 'react'
import {EmailIntegration} from 'models/integration/types'

type Props = {
    integration?: EmailIntegration | undefined
}

export default function EmailIntegrationOnboarding({integration: __}: Props) {
    return <></>
}
