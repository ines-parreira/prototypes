// TODO: Remove this re-export — update 106 consumers to import from @repo/api-resources directly
export { appQueryClient, queryCache } from '@repo/api-resources'
export type { Meta } from '@repo/api-resources'
