export interface TableColumn {
    className?: string
    title: string
    tooltip?: string
}

export enum ABGroupValueFormat {
    Number = 'number',
    Percentage = 'percentage',
    Date = 'date,',
}
